<Window x:Class="RUZ.NARFU.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RUZ.NARFU"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <ItemsPanelTemplate x:Key="cnv">
            <Canvas/>
        </ItemsPanelTemplate>
    </Window.Resources>
    <Grid>
        <Viewbox>
            <Canvas/>
        </Viewbox>
        <ItemsControl Name="ic"  ItemsPanel="{StaticResource cnv}" Grid.ColumnSpan="2" Grid.Row="2"  >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Height="50" Width="50" Margin="{Binding Margin}" BorderThickness="1" BorderBrush="#e3e3e3">
                        <Rectangle  Fill="#ffcdcd" />
                    </Border>

                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
