<Window x:Class="RUZ.NARFU.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RUZ.NARFU"
        mc:Ignorable="d"
        Title="Ruz.NARFU" Height="1000" Width="1400" ResizeMode="NoResize" >
    <Window.Resources>
        <local:TypeToColor x:Key="TypeConverter"/>
        <ItemsPanelTemplate x:Key="cnv">
            <Canvas/>
        </ItemsPanelTemplate>
    </Window.Resources>
    <Grid>
        <Viewbox>
            <Canvas/>
        </Viewbox>
        <ItemsControl Name="ic"  ItemsPanel="{StaticResource cnv}" Grid.ColumnSpan="2" Grid.Row="2"  >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Height="150" Width="230" Background="{Binding ElementName=type, Path=Text, Converter={StaticResource TypeConverter} }" Margin="{Binding Margin}" BorderThickness="1" BorderBrush="#e3e3e3">
                        <StackPanel>
                            <TextBlock Text="{Binding Num}"/>
                            <TextBlock Text="{Binding Time}"/>
                            <TextBlock Name="type" Text="{Binding Type}"/>
                            <TextBlock Text="{Binding Place}"/>
                            <TextBlock Text="{Binding Name}"/>
                            <TextBlock Text="{Binding Lecturer}"/>
                            <TextBlock Text="{Binding Class}"/>
                            <TextBlock Text="{Binding Group}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
